import classes from "./AboutPage.module.css"

const AboutPage = (props) => {

    return (
        <div className={classes.AboutPage}>
            <h2>About us</h2>
        </div>
    );
  };
  
  export { AboutPage };